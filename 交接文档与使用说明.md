# 项目概览

本项目是一套面向船舶场景的多源感知链路，围绕多相机视频、AIS、GNSS 三类数据完成采集、解析、融合跟踪与全景结果输出。

当前仓库可以分成 3 条主线：

- 实时链路：实时接入 RTSP 相机、AIS、GNSS，输出融合轨迹和全景结果。
- 离线回放链路：从离线视频、AIS CSV、固定 GNSS 参数构造 ROS2 数据流，复现融合跟踪结果。
- 数据录制链路：独立录制 AIS、GNSS、RTSP 多相机数据，为离线回放或数据集制作提供原始输入。

文档原则：

- 以当前代码、`CMakeLists.txt`、`setup.py`、launch、YAML 配置为准。
- 不以旧 README、旧调试文档中的节点名或话题名为准。
- 同时服务 后续维护者 和 前端对接人员。

# 系统运行链路

## 实时链路

```text
RTSP 相机
  -> image_stitching_pkg/JH_rtsp
  -> /rtsp_image_0 ~ /rtsp_image_2

AIS 设备
  -> marnav_ais/ais_parser_batch_pub_node
  -> /ais_batch_topic_realtime

GNSS 设备
  -> marnav_ais/gnss_parser_pub_node
  -> /gnss_topic

/rtsp_image_0 ~ /rtsp_image_2 + /ais_batch_topic_realtime + /gnss_topic
  -> marnav_vis/DeepSORVF_JH
  -> /fus_trajectory_topic

/rtsp_image_0 ~ /rtsp_image_2 + /fus_trajectory_topic + /gnss_topic
  -> image_stitching_pkg/JH_ROS_stitch
  -> /image_topic_all (mycustface/JHjpg)
```

说明：

- 整体运行前需要启动虚拟环境: `conda activate yolov11`
- `DeepSORVF_JH` 负责视觉检测、AIS/GNSS 融合和轨迹输出。
- `JH_ROS_stitch` 负责多相机拼接，并把拼接图像和轨迹/GNSS 元数据一起封装为 `JHjpg`。

## 离线回放链路

```text
离线视频
  -> marnav_vis/camera_pub_node
  -> /camera_image_topic_0 ~ /camera_image_topic_2

离线 AIS CSV
  -> marnav_vis/ais_csv_pub_node
  -> /ais_csv_topic
  -> marnav_vis/ais_sorted_pub_node
  -> /ais_batch_topic_offline

固定 GNSS 参数
  -> marnav_vis/gnss_pub_node
  -> /gnss_topic

/camera_image_topic_0 ~ /camera_image_topic_2 + /ais_batch_topic_offline + /gnss_topic
  -> marnav_vis/DeepSORVF_JH
  -> /fus_trajectory_topic

如需输出全景结果：
/camera_image_topic_0 ~ /camera_image_topic_2 + /fus_trajectory_topic + /gnss_topic
  -> image_stitching_pkg/JH_ROS_stitch
  -> /image_topic_all
```

说明：

- 离线回放默认由 `marnav_vis` 伪装出相机、AIS、GNSS ROS 话题。
- 若只验证跟踪链路，可只启动 `marnav_vis` 的离线 launch。
- 若前端需要离线全景结果，还需额外启动 `image_stitching_pkg` 的拼接节点。

## RosBag 伪实时回放链路（新增）

```text
ros2 bag play (回放图像/AIS/GNSS)
  -> /rtsp_image_0 ~ /rtsp_image_2 + /ais_batch_topic_realtime + /gnss_topic
  -> marnav_vis/DeepSORVF_JH
  -> /fus_trajectory_topic

/rtsp_image_0 ~ /rtsp_image_2 + /fus_trajectory_topic + /gnss_topic
  -> image_stitching_pkg/JH_ROS_stitch
  -> /image_topic_all
```

说明：

- 用途是“伪实时联调”：数据来自 rosbag，但链路形态尽量贴近实时系统。
- 推荐入口：`./scripts/run_stitch_track_rosbag.sh`（内部调用 `JH_Stitch_Track_RosBag_launch.py`）。

## 数据录制链路

```text
AIS + GNSS + RTSP 相机
  -> dataset_recorder/main.py
  -> session_xxx/ais + session_xxx/gnss + session_xxx/camera + metadata.json
```

说明：

- 这条链路用于生成原始数据，不直接参与 ROS2 实时前端输出。

# 功能包关系总览

| 包名 | 主要职责 | 上游输入 | 下游输出 | 关键配置 / 入口 |
| --- | --- | --- | --- | --- |
| `dataset_recorder` | 录制 AIS、GNSS、RTSP 数据 | 串口 / TCP / UDP / RTSP | 本地 `session_xxx` 目录 | `main.py`、`config.yaml` |
| `image_stitching_pkg` | RTSP 转 ROS 图像；多相机拼接并输出全景消息 | RTSP、图像 topic、`VisiableTraBatch`、`Gnss` | `/rtsp_image_*`、`/image_topic_all` | `JH_rtsp`、`JH_ROS_stitch`、`JH_rtsp_config.yaml`、`JH_stitch_config.yaml`、`JH_stitch_rosbag_config.yaml`、`JH_Stitch_Track_RosBag_launch.py` |
| `marnav_ais` | 解析实时 AIS 与 GNSS | 串口 / TCP / UDP | `AisBatch`、`Gnss` | `ais_parser_batch_pub_node`、`gnss_parser_pub_node` |
| `marnav_interfaces` | 定义 ROS2 消息契约 | 无 | 被其他包引用 | `msg/*.msg` |
| `marnav_vis` | 离线数据发布；视觉检测、AIS/GNSS 融合跟踪 | 图像 topic、`AisBatch`、`Gnss`、离线视频 / CSV | `VisiableTraBatch`，并在本地窗口显示结果 | `DeepSORVF_JH`、`camera_pub_node`、`ais_csv_pub_node`、`ais_sorted_pub_node`、`gnss_pub_node` |
| `mycustface` | 定义自定义图像消息 | 无 | 被 `image_stitching_pkg` 使用 | `msg/JHjpg.idl`、`msg/Header.idl` |

# dataset_recorder

## 包定位

`dataset_recorder` 是独立 Python 录制工具，不是 ROS2 功能包。它负责直接接入 AIS、GNSS、RTSP 多相机，把原始输入保存为本地数据集，供后续离线处理或数据集制作使用。

它不负责发布 ROS2 前端实时接口，也不参与 `JHjpg` 全景结果输出。

## 启动入口与配置文件

- 主入口：`src/dataset_recorder/main.py`
- 配置文件模板：`src/dataset_recorder/config_template.yaml`
- 实际运行配置：`src/dataset_recorder/config.yaml`
- 运行方式：

```bash
cd /home/tl/RV/src/dataset_recorder
python3 main.py --config config.yaml --log-level INFO
```

配置重点：

- `devices.ais`：AIS 通信方式与串口 / TCP / UDP 参数。
- `devices.gnss`：GNSS UDP 监听端口、质量阈值、姿态偏置。
- `devices.cameras`：RTSP 地址、相机 ID、帧率、内参。
- `storage.root_path`：录制数据根目录。

## 输入 / 输出接口

输入：

- AIS：`serial` / `tcp` / `udp`
- GNSS：当前主配置为 UDP
- 相机：RTSP

输出：

- 目录命名：`session_YYYY_MM_DD_HH_MM_SS`
- 子目录：
  - `gnss/`
  - `ais/`
  - `camera/`
- 会话元数据：`metadata.json`

当前主流程实际行为：

- `main.py` 会创建 `session_xxx` 目录并初始化各接收器。
- AIS 和 GNSS 以 CSV 形式持续写盘。
- 相机数据按相机 ID 保存到 `camera/` 子目录。

## 修改时优先查看的关键文件

- `src/dataset_recorder/main.py`
- `src/dataset_recorder/config_template.yaml`
- `src/dataset_recorder/ais_receiver.py`

## 约束、风险与已知坑点

- 这是独立工具链，不会自动接入当前 ROS2 实时前端链路。
- 当前交接应以 `main.py` 创建的 `session_xxx` 目录结构为准。
- `data_storage.py` 中存在 `clip_xxx` 目录设计，但当前主流程并未调用该模块，维护时不要把它误认为实际运行标准。

# image_stitching_pkg

## 包定位

`image_stitching_pkg` 负责两件事：

- `JH_rtsp`：把 RTSP 相机流转成 ROS2 图像 topic。
- `JH_ROS_stitch`：订阅多路图像、融合轨迹和 GNSS，输出拼接后的全景消息 `mycustface/JHjpg`。

它是前端拿到全景图像和叠加结果的最终出口。

在新设备中，用户首先需要优先读取环境变量 RV_WS:`（需在 .bashrc 中执行: export RV_WS=/home/xxx/RV & source ~/.bashrc）`

## 启动入口与配置文件

当前真实可用入口：

- 可执行文件：
  - `JH_rtsp`
  - `JH_ROS_stitch`
- 实时 launch：
  - `src/image_stitching_pkg/launch/JH_Stitch_Track_Realtime_launch.py`
- 离线拼接 launch：
  - `src/image_stitching_pkg/launch/JH_Stitch_Track_Offline_launch.py`
- RosBag 伪实时整体 launch：
  - `src/image_stitching_pkg/launch/JH_Stitch_Track_RosBag_launch.py`
- 配置文件：
  - `src/image_stitching_pkg/config/JH_rtsp_config.yaml`
  - `src/image_stitching_pkg/config/JH_stitch_config.yaml`
  - `src/image_stitching_pkg/config/JH_stitch_rosbag_config.yaml`

配置职责：

- `JH_rtsp_config.yaml`：定义 RTSP 地址、相机 ID、图像输出 topic。
- `JH_stitch_config.yaml`：定义拼接节点订阅的相机 topic、输出 topic、轨迹 topic、GNSS topic，以及拼接参数、裁剪参数、缓存和标定路径；也包含轨迹去重参数。
- `JH_stitch_rosbag_config.yaml`：除拼接参数外，还定义 `Rosbag_parameters.rosbag_path`、`Rosbag_parameters.publish_topics`，用于 RosBag 伪实时回放。

## 输入 / 输出接口

### `JH_rtsp`

输入：

- RTSP 地址，默认在 `JH_rtsp_config.yaml` 中配置 3 路相机。

输出：

- `/rtsp_image_0`
- `/rtsp_image_1`
- `/rtsp_image_2`

消息类型：

- `sensor_msgs/Image`

### `JH_ROS_stitch`

输入：

- 3 路图像 topic
- `/fus_trajectory_topic`，类型 `marnav_interfaces/VisiableTraBatch`
- `/gnss_topic`，类型 `marnav_interfaces/Gnss`

输出：

- `/image_topic_all`，类型 `mycustface/JHjpg`

`JHjpg` 内部内容：

- `mheader`：自定义头，含时间戳与 ID
- `picture`：JPEG 二进制
- `message`：JSON 字符串，包含 GNSS 与轨迹元数据

### 轨迹去重（新增）

`JH_ROS_stitch` 在生成 `JHjpg.message.trajectories` 前，新增了跨相机重复框去重逻辑。

注意：该功能仅在启动 `image_stitching_pkg/JH_ROS_stitch` 并输出 `JHjpg` 时生效。

- 生效范围：仅影响 `/image_topic_all` 中的 `JHjpg.message.trajectories`；不修改 `/fus_trajectory_topic` 原始发布内容。
- 去重规则：
  - 两框 `mmsi > 0` 且相同，判定为重复候选。
  - 至少一方 `mmsi <= 0` 时，仅当 `IoU >= 0.70` 判定为重复候选。
  - 两框 `mmsi > 0` 且不同，不合并。
- 保留策略：优先保留更靠画面中心的框；同分时保留面积更大者，再同分保留有有效 `mmsi` 的框。
- 绘制联动：`draw_box_or_not` 只控制“是否把去重后的框画到拼接图像上”；即使关闭绘制，`JHjpg.message.trajectories` 仍输出去重结果。
- 对应配置参数（`JH_stitch_config.yaml` / `JH_stitch_rosbag_config.yaml`）：
  - `trajectory_dedup_enable`
  - `trajectory_dedup_iou_threshold_no_mmsi`（默认 `0.70`）
  - `trajectory_dedup_keep_policy`（当前支持 `"center"`）

## 修改时优先查看的关键文件

- `src/image_stitching_pkg/src/JH_rtsp.cpp`
- `src/image_stitching_pkg/src/JH_ros2_stitch_node.cpp`
- `src/image_stitching_pkg/config/JH_stitch_config.yaml`

## 约束、风险与已知坑点

- 当前实现固定只支持 3 个相机，配置文件中相机数量不为 3 会直接报错。
- `JH_ROS_stitch` 依赖 `RV_WS` 环境变量或包路径推导来拼接缓存 / 标定路径，推荐通过仓库脚本启动。
- `JHjpg.message` 的内容由 C++ 节点实时拼装，前端应按 JSON 解析，不要把它当普通日志字符串。

# marnav_ais

## 包定位

`marnav_ais` 负责实时接入和解析 AIS、GNSS 数据，并向 ROS2 主链路发布标准消息。当前交接时只应关注两个真实可用入口：

- `ais_parser_batch_pub_node`
- `gnss_parser_pub_node`

## 启动入口与配置文件

当前真实可用入口：

- `ais_parser_batch_pub_node`
- `gnss_parser_pub_node`

对应源码：

- `src/marnav_ais/src/ais_parser_batch_pub_node.cpp`
- `src/marnav_ais/src/gnss_parser_pub_node.cpp`

配置来源：

- 默认通过 `config_file` 参数读取 YAML。
- 当前默认回退到 `marnav_vis/config/track_realtime_config.yaml`，不是 `marnav_ais` 自己的独立配置目录。

支持的通信方式：

- `serial`
- `tcp`
- `udp`

## 输入 / 输出接口

### AIS 解析节点

输入：

- AIS 设备原始数据流

输出：

- 默认 topic：`/ais_batch_topic_realtime`
- 消息类型：`marnav_interfaces/AisBatch`

行为说明：

- 当前以 AIVDM 为主，且主逻辑只处理 AIS 1 / 2 / 3 类位置报文。
- 发布模式是批量发布，不是单条实时直发。

### GNSS 解析节点

输入：

- GNSS 设备原始数据流

输出：

- 默认 topic：`/gnss_topic`
- 消息类型：`marnav_interfaces/Gnss`

## 修改时优先查看的关键文件

- `src/marnav_ais/src/ais_parser_batch_pub_node.cpp`
- `src/marnav_ais/src/gnss_parser_pub_node.cpp`
- `src/marnav_ais/CMakeLists.txt`

## 约束、风险与已知坑点

- 文档和联调时不要沿用这个功能包中 `README.md` 中的旧节点名 `ais_parser_node_v2`。
- 旧文档中出现的 `/ais_batch_data` 不是当前主链路默认 topic，当前代码和 YAML 使用的是 `/ais_batch_topic_realtime`。
- `marnav_ais` 当前没有独立的正式配置目录，维护时要注意它实际上是依赖 `marnav_vis` 的实时配置文件。

# marnav_interfaces

## 包定位

`marnav_interfaces` 是整个系统的消息契约包。前端、融合逻辑、拼接逻辑和实时解析都通过这里定义的消息结构对齐。

联调时，这个包应作为第一参考，而不是去不同节点代码里零散查字段。

## 启动入口与配置文件

该包不单独运行，没有 launch。它通过 `rosidl_generate_interfaces` 生成消息类型，供其他包编译依赖。

关键文件：

- `src/marnav_interfaces/CMakeLists.txt`
- `src/marnav_interfaces/msg/*.msg`

## 输入 / 输出接口

### 消息总表

| 消息类型 | 当前用途 | 主要生产者 | 主要消费者 | 备注 |
| --- | --- | --- | --- | --- |
| `Ais` | 单条 AIS 数据 | `marnav_ais`、`marnav_vis/ais_csv_pub_node` | `AisBatch` 组装逻辑 | 主链路通常不直接给前端消费 |
| `AisBatch` | 一批 AIS 数据 | `marnav_ais/ais_parser_batch_pub_node`、`marnav_vis/ais_sorted_pub_node` | `marnav_vis/DeepSORVF_JH` | 主链路 AIS 输入 |
| `Gnss` | 相机位姿 / 位置信息 | `marnav_ais/gnss_parser_pub_node`、`marnav_vis/gnss_pub_node` | `DeepSORVF_JH`、`JH_ROS_stitch` | 主链路 GNSS 输入 |
| `VisiableTra` | 单条融合轨迹 / 检测框 | `DeepSORVF_JH` | `VisiableTraBatch` 组装逻辑、`JH_ROS_stitch` | bbox 坐标为像素坐标 |
| `VisiableTraBatch` | 一批融合轨迹 | `DeepSORVF_JH` | `JH_ROS_stitch` | 主链路轨迹输出 |
| `AisVis` | AIS 与图像坐标的组合消息 | 当前主链路未使用 | 历史 / 预留 | 不作为当前对接标准 |
| `Gga` | GGA 句子解析结果 | 当前主链路未启用 | 历史 / 预留 | 仅消息定义仍保留 |
| `Rmc` | RMC 句子解析结果 | 当前主链路未启用 | 历史 / 预留 | 仅消息定义仍保留 |

### 前端和联调重点消息

| 消息类型 | 关键字段 | 单位 / 含义 | 默认 topic |
| --- | --- | --- | --- |
| `Ais` | `mmsi` `lat` `lon` `speed` `course` `heading` `type` `timestamp` | 经纬度、速度、航向、AIS 类型、时间戳 | 不固定，通常作为中间消息 |
| `AisBatch` | `ais_list` `batch_time` | 一秒左右的一批 AIS 数据 | `/ais_batch_topic_realtime` 或 `/ais_batch_topic_offline` |
| `Gnss` | `latitude` `longitude` `horizontal_orientation` `vertical_orientation` `camera_height` `timestamp` | 相机位置、姿态、高度 | `/gnss_topic` |
| `VisiableTra` | `camera_name` `ais` `mmsi` `ship_type` `sog` `cog` `lat` `lon` `box_x1/y1/x2/y2` | 单条轨迹与检测框，框坐标为像素 | 通常作为批量消息元素 |
| `VisiableTraBatch` | `visiable_tra_list` `timestamp` | 一批轨迹结果 | `/fus_trajectory_topic` |

## 修改时优先查看的关键文件

- `src/marnav_interfaces/msg/Ais.msg`
- `src/marnav_interfaces/msg/Gnss.msg`
- `src/marnav_interfaces/msg/VisiableTraBatch.msg`

## 约束、风险与已知坑点

- 只要修改消息字段，就会影响 `marnav_ais`、`marnav_vis`、`image_stitching_pkg` 三条链路。
- 前端不应直接以 `AisVis`、`Gga`、`Rmc` 作为当前主链路标准输入，这些更接近历史接口或预留接口。

# marnav_vis

## 包定位

`marnav_vis` 有两类职责：

- 离线数据发布：把离线视频、AIS CSV、固定 GNSS 参数转成 ROS2 话题。
- 核心融合跟踪：通过 `DeepSORVF_JH` 完成视觉检测、AIS/GNSS 融合和轨迹输出。

它是“融合结果”的核心包，但不是全景图最终出口。

## 启动入口与配置文件

当前真实可用入口：

- `DeepSORVF_JH`
- `camera_pub_node`
- `gnss_pub_node`
- `ais_csv_pub_node`
- `ais_sorted_pub_node`

对应配置文件：

- `src/marnav_vis/config/track_realtime_config.yaml`
- `src/marnav_vis/config/track_offline_config.yaml`

角色区分：

- `track_realtime_config.yaml`：实时链路配置，含 RTSP topic、AIS 接入方式、GNSS 接入方式、相机内参、融合参数。
- `track_offline_config.yaml`：离线链路配置，含视频路径、AIS CSV 目录、离线 GNSS 参数、相机 topic、融合参数。

## 输入 / 输出接口

### 离线模式

主要节点：

- `camera_pub_node`
- `gnss_pub_node`
- `ais_csv_pub_node`
- `ais_sorted_pub_node`

行为说明：

- `camera_pub_node` 读取离线视频并按设定帧率发布图像 topic。
- `gnss_pub_node` 周期性发布固定或带微小扰动的 GNSS 参数。
- `ais_csv_pub_node` 从 AIS CSV 文件夹中读取单条 AIS 数据。
- `ais_sorted_pub_node` 把离线单条 AIS 重新整理成 `AisBatch`。

默认输出：

- `/camera_image_topic_0` ~ `/camera_image_topic_2`
- `/gnss_topic`
- `/ais_csv_topic`
- `/ais_batch_topic_offline`

### 实时与离线共用的融合模式

核心节点：

- `DeepSORVF_JH`

输入：

- 多路图像 topic
- `AisBatch`
- `Gnss`

输出：

- `/fus_trajectory_topic`
- 消息类型：`marnav_interfaces/VisiableTraBatch`

行为说明：

- 在主进程里做 YOLO 检测。
- 使用多进程处理各相机的 AIS / VIS / FUS / DRAW 逻辑。
- 每秒发布一批 `VisiableTra` 结果。

## 修改时优先查看的关键文件

- `src/marnav_vis/marnav_vis/DeepSORVF_ros_v7.py`
- `src/marnav_vis/config/track_realtime_config.yaml`
- `src/marnav_vis/config/track_offline_config.yaml`

## 约束、风险与已知坑点

- `setup.py` 中声明了 `DeepSORVF_ros = marnav_vis.DeepSORVF_ros_v6:main`，但仓库中不存在 `DeepSORVF_ros_v6.py`，不要把它当作当前可用入口。
- `README_CONFIG.md` 中提到的 `Assemble_JH_launch.py`、`camera_pub_temporary_Test_node` 与当前仓库实际入口不一致，应以当前 `launch/*.py` 和 `setup.py` 为准。
- 离线 `camera_pub.py` 的注释与当前实现不完全一致，维护时以代码行为为准，不以注释表述为准。

# mycustface

## 包定位

`mycustface` 是自定义消息包，当前最重要的用途是定义前端需要消费的 `JHjpg` 全景消息。

## 启动入口与配置文件

该包不单独运行，没有 launch。它通过 `rosidl_generate_interfaces` 生成消息类型，供 `image_stitching_pkg` 使用。

关键消息定义：

- `src/mycustface/msg/JHjpg.idl`
- `src/mycustface/msg/Header.idl`
- `src/mycustface/msg/ImageMsg.msg`

## 输入 / 输出接口

### `JHjpg`

字段结构：

- `mheader`
  - `timestamp`：自定义时间戳
  - `id`：自定义 ID
- `index`：序号
- `message`：JSON 字符串
- `size`：JPEG 字节数
- `picture`：JPEG 二进制数组

当前生产者：

- `image_stitching_pkg/JH_ROS_stitch`

当前消费者：

- 前端桥接层或任意订阅 `/image_topic_all` 的下游系统

### 其他消息

- `ImageMsg`
- `MyCustomMsg`

说明：

- 这两个消息目前更接近辅助 / 历史接口。
- 当前前端如果要接拼接图像和轨迹元数据，应以 `JHjpg` 为准。

## 修改时优先查看的关键文件

- `src/mycustface/msg/JHjpg.idl`
- `src/mycustface/msg/Header.idl`
- `src/mycustface/CMakeLists.txt`

## 约束、风险与已知坑点

- 前端若通过桥接层转发 `JHjpg`，必须确认 `picture` 按二进制 / 字节数组传输，不要误当 UTF-8 字符串。
- `message` 是标准 JSON 字符串，不是 ROS2 子消息。

# 前端对接速查

## Topic 速查表

| 用途 | 默认 topic | 消息类型 | 来源 |
| --- | --- | --- | --- |
| 实时图像输入 | `/rtsp_image_0` ~ `/rtsp_image_2` | `sensor_msgs/Image` | `image_stitching_pkg/JH_rtsp` |
| 离线图像输入 | `/camera_image_topic_0` ~ `/camera_image_topic_2` | `sensor_msgs/Image` | `marnav_vis/camera_pub_node` |
| GNSS | `/gnss_topic` | `marnav_interfaces/Gnss` | `marnav_ais/gnss_parser_pub_node` 或 `marnav_vis/gnss_pub_node` |
| 实时 AIS 批量 | `/ais_batch_topic_realtime` | `marnav_interfaces/AisBatch` | `marnav_ais/ais_parser_batch_pub_node` |
| 离线 AIS 批量 | `/ais_batch_topic_offline` | `marnav_interfaces/AisBatch` | `marnav_vis/ais_sorted_pub_node` |
| 融合轨迹 | `/fus_trajectory_topic` | `marnav_interfaces/VisiableTraBatch` | `marnav_vis/DeepSORVF_JH` |
| 拼接输出 | `/image_topic_all` | `mycustface/JHjpg` | `image_stitching_pkg/JH_ROS_stitch` |

## 消息类型速查表

| 消息类型 | 前端是否需要直接消费 | 说明 |
| --- | --- | --- |
| `marnav_interfaces/Gnss` | 可选 | 需要单独显示位姿 / 位置时使用 |
| `marnav_interfaces/AisBatch` | 通常否 | 更多作为融合节点输入 |
| `marnav_interfaces/VisiableTraBatch` | 可选 | 需要单独显示融合框和轨迹时使用 |
| `mycustface/JHjpg` | 是 | 当前前端接收全景图和附带元数据的推荐标准 |

## 默认 topic 名汇总

- 实时图像：`/rtsp_image_0`、`/rtsp_image_1`、`/rtsp_image_2`
- 离线图像：`/camera_image_topic_0`、`/camera_image_topic_1`、`/camera_image_topic_2`
- GNSS：`/gnss_topic`
- 实时 AIS 批量：`/ais_batch_topic_realtime`
- 离线 AIS 批量：`/ais_batch_topic_offline`
- 融合轨迹：`/fus_trajectory_topic`
- 拼接输出：`/image_topic_all`

## `JHjpg` 的消费方式

推荐把 `JHjpg` 理解为“图像 + 元数据”组合消息：

- `picture`：前端解码为 JPEG 图像。
- `message`：前端按 JSON 解析，用于拿到 GNSS 与轨迹框。
- `mheader` / `index` / `size`：用于日志、调试和数据完整性校验。

若前端只关心结果展示，优先消费 `JHjpg`，而不是自己再去组合 `/gnss_topic`、`/fus_trajectory_topic` 和图像 topic。

## `JHjpg.message` JSON 示例

```json
{
  "gnss": {
    "latitude": 30.60138888,
    "longitude": 114.32583333,
    "horizontal_orientation": 7.0,
    "vertical_orientation": -4.0,
    "camera_height": 20.0
  },
  "trajectories": [
    {
      "message_type": "AIS",
      "ship_type": "vessel",
      "x1": 120,
      "y1": 220,
      "x2": 260,
      "y2": 340,
      "mmsi": 413000000,
      "sog": 12.4,
      "cog": 87.5,
      "lat": 30.6015,
      "lon": 114.3261
    }
  ]
}
```

字段说明：

- `gnss.*`：拼接时刻对应的 GNSS 信息。
- `trajectories[]`：拼接图中的轨迹框和船舶属性。
- `trajectories[]` 是拼接节点去重后的结果（跨相机重复框已按规则合并）。
- `message_type`：当前代码中为 `"AIS"` 或 `"Visual"`。
- `x1/y1/x2/y2`：像素坐标。
- `mmsi/sog/cog/lat/lon`：来自融合结果，前端可直接展示。

# 启动方式

## 实时链路推荐启动方式

推荐使用仓库脚本：

```bash
cd /home/tl/RV
./scripts/run_stitch_track_realtime.sh
```

该脚本会：

- 检查 ROS2 环境
- 检查 `install/`
- 自动导出 `RV_WS`
- 启动 `src/image_stitching_pkg/launch/JH_Stitch_Track_Realtime_launch.py`

## 离线链路 - RosBag 推荐启动方式
若验证事先录制的多相机ros包的离线跟踪链路，使用现有脚本：
```bash
cd /home/tl/RV
./scripts/run_stitch_track_rosbag.sh
```

该脚本会：

- 检查 ROS2 环境与 `install/` 目录
- 自动导出 `RV_WS`
- 执行：`ros2 launch image_stitching_pkg JH_Stitch_Track_RosBag_launch.py`

`JH_Stitch_Track_RosBag_launch.py` 会同时拉起：

- `image_stitching_pkg/JH_ROS_stitch`
- `marnav_vis/DeepSORVF_JH`
- `ros2 bag play`（延时启动，默认 `bag_start_delay:=3.0`，用于等待订阅就绪）

RosBag 路径与话题说明：

- 默认从 `image_stitching_pkg/config/JH_stitch_rosbag_config.yaml` 读取：
  - `Rosbag_parameters.rosbag_path`
  - `Rosbag_parameters.publish_topics`
- 也支持命令行覆盖：`rosbag_path:=...`
- `rosbag_path` 允许三种形式：bag 目录、`metadata.yaml` 文件路径、`.db3` 文件路径（launch 内会自动归一化到 bag 目录）
- launch 会校验 `JH_stitch_rosbag_config.yaml` 与 `marnav_vis/config/track_rosbag_config.yaml` 的相机/GNSS 话题是否对齐，不对齐会直接报错

## 离线链路 - Fvessel数据集 推荐启动方式

若只验证离线跟踪链路，使用现有脚本：

```bash
cd /home/tl/RV
./scripts/run_stitch_track_offline.sh
```

该脚本会启动：

- `src/marnav_vis/launch/Assemble_track_offline_launch.py`

当前 Fvessel 离线路径的说明：

- 该路径默认只跑 `marnav_vis` 离线跟踪，不启动 `JH_ROS_stitch`，因此不会触发拼接侧去重逻辑。
- Fvessel 原始数据本质是单相机朝向数据，当前链路中将其复制为 3 路仅用于模拟多相机流程，不代表真实多视角重叠场景。
- 因此该离线场景下“跨相机重叠去重”不作为效果评估项。


## 单独启动节点时的优先原则

- 优先通过 launch 启动，不要手工分散传参。
- 若必须单独启动节点，配置文件优先使用：
  - 实时：`marnav_vis/config/track_realtime_config.yaml`
  - 离线：`marnav_vis/config/track_offline_config.yaml`
  - 拼接：`image_stitching_pkg/config/JH_stitch_config.yaml`
  - RTSP：`image_stitching_pkg/config/JH_rtsp_config.yaml`

# 已知问题与交接注意事项

## 总原则

- 以代码、`CMakeLists.txt`、`setup.py`、launch、YAML 为准。
- 不要直接照抄旧 README、旧调试文档中的节点名、topic 名和 launch 名。

## 当前已确认的过时项

### `marnav_ais` 旧文档

- `src/marnav_ais/README_v2.md` 中使用了 `ais_parser_node_v2`、`/ais_batch_data` 等旧名称。
- `src/marnav_ais/DEBUG_GUIDE.md` 也围绕 `ais_parser_node_v2` 展开。
- 当前代码实际编译出的入口是 `ais_parser_batch_pub_node`，默认 topic 来自 YAML，当前主链路是 `/ais_batch_topic_realtime`。

### `marnav_vis` 旧文档与旧入口

- `src/marnav_vis/README_CONFIG.md` 中提到的 `Assemble_JH_launch.py` 在当前仓库不存在。
- 其中提到的 `camera_pub_temporary_Test_node` 也不是当前 `setup.py` 中的实际入口。
- 当前有效入口应以 `setup.py` 的 `console_scripts` 和 `launch/` 目录内文件为准。

### `marnav_vis/setup.py`

- `setup.py` 中声明了 `DeepSORVF_ros = marnav_vis.DeepSORVF_ros_v6:main`。
- 当前仓库没有 `DeepSORVF_ros_v6.py`，因此文档和联调时不要把这个入口视为可用。

### `dataset_recorder` 中的历史设计残留

- `data_storage.py` 里有 `clip_xxx` 目录命名和批量写盘设计。
- 当前主流程 `main.py` 实际创建的是 `session_xxx` 目录。
- 交接时统一以 `main.py` 的实际行为为准。

## 后续维护建议

- 改 AIS 接入参数：先看 `track_realtime_config.yaml` 和 `marnav_ais` 的两个解析节点。
- 改拼接参数：先看 `JH_stitch_config.yaml` 和 `JH_ros2_stitch_node.cpp`。
- 改拼接去重策略：先看 `JH_stitch_config.yaml`（或 `JH_stitch_rosbag_config.yaml`）中的 `trajectory_dedup_*`，再看 `JH_SeamStitcher.cpp` 的去重实现。
- 改前端输出：先看 `JHjpg.idl` 和 `JH_ros2_stitch_node.cpp` 中 `JHmessagetoJson()` 的 JSON 组装逻辑。
- 改融合逻辑：先看 `DeepSORVF_ros_v7.py` 和 `utils_JH/`。
- 改 RosBag 联调入口：先看 `scripts/run_stitch_track_rosbag.sh` 与 `JH_Stitch_Track_RosBag_launch.py`。
